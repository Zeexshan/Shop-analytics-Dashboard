# Required Environment Variables for Production
# CRITICAL: All variables below are REQUIRED for secure operation

# JWT Token Security (minimum 32 characters, use crypto.randomBytes(32).toString('hex'))
JWT_SECRET=your-secure-random-jwt-secret-here

# Admin Authentication (bcrypt hash of admin password)
ADMIN_PASSWORD_HASH=your-bcrypt-hashed-admin-password

# Admin Reset Security (secure random string for password reset authorization)
ADMIN_RESET_CODE=your-secure-admin-reset-code

# Gumroad Integration (get from your Gumroad product settings)
GUMROAD_PRODUCT_PERMALINK=your-gumroad-permalink
GUMROAD_PRODUCT_ID=your-gumroad-product-id

# License Security Salts (use crypto.randomBytes(32).toString('hex'))
LICENSE_HASH_SALT=your-license-hash-salt
DEVICE_HASH_SALT=your-device-hash-salt

# Optional: Database URL (for production PostgreSQL)
# DATABASE_URL=postgresql://user:pass@host:port/dbname
